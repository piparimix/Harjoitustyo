<Window
    x:Class="Harjoitustyö.Päävalikko"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Harjoitustyö"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Rakennus OY - Päävalikko"
    Width="800"
    Height="500"
    MinWidth="600"
    MinHeight="400"
    Background="GhostWhite"
    mc:Ignorable="d">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="LightGray" />
            <Setter Property="Foreground" Value="#333333" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Width" Value="200" />
            <Setter Property="Height" Value="60" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="10"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="5"
                                    Opacity="0.1"
                                    ShadowDepth="2"
                                    Color="Gray" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="WhiteSmoke" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="0,40,0,20"
            HorizontalAlignment="Center">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="36"
                FontWeight="Bold"
                Foreground="Black"
                Text="Rakennus Oy" />
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="24"
                FontWeight="Light"
                Foreground="DimGray"
                Text="Laskutussovellus" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Top">

            <UniformGrid Columns="2">
                <Button
                    x:Name="Uusi_Lasku"
                    Click="Uusi_Lasku_Click"
                    Content="Uusi Lasku" />

                <Button
                    x:Name="Kaikki_Lasku"
                    Click="Kaikki_Lasku_Click"
                    Content="Kaikki Laskut" />
                <Button
                    x:Name="Tuotteet"
                    Click="Tuotteet_Click"
                    Content="Tuotelista" />

                <Button
                    x:Name="Muokkaa_Lasku"
                    Click="Muokkaa_Lasku_Click"
                    Content="Muokkaa Laskua" />


            </UniformGrid>

            <Button
                x:Name="Sulje"
                HorizontalAlignment="Center"
                Click="Sulje_Click"
                Content="Sulje sovellus" />

        </StackPanel>
    </Grid>
</Window>